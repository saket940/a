{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "workspaces": [
    "artifacts/api-server",
    "artifacts/internhub",
    "lib/api-client-react",
    "lib/api-spec",
    "lib/api-zod",
    "lib/db",
    "scripts"
  ],
  "scripts": {
    "dev": "bash -lc 'npm run dev:server & npm run dev:client & wait'",
    "dev:server": "npm run dev -w @workspace/api-server",
    "dev:client": "npm run dev -w @workspace/internhub",
    "build": "npm run build -w @workspace/api-server && npm run build -w @workspace/internhub",
    "typecheck:libs": "tsc --build",
    "typecheck": "npm run typecheck:libs && npm run typecheck -w @workspace/api-server && npm run typecheck -w @workspace/internhub"
  },
  "private": true,
  "devDependencies": {
    "typescript": "~5.9.2",
    "prettier": "^3.8.1"
  }
}
